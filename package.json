{
  "name": "huellas-unidas",
  "version": "1.0.0",
  "private": true,
  "description": "Plataforma comunitaria para el reencuentro de mascotas perdidas y la adopción responsable.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "migrate": "node scripts/migrate.js",
    "seed": "node scripts/seed.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2"
  }
}
